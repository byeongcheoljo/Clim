<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="movies">
	<select id="searchMovieListForCliming" resultType="Movie"
		parameterType="String">
		SELECT no, title, runtime, poster
		FROM movies
		WHERE title
		= #{title}
	</select>

	<select id="selectMovieTrailer" resultType="Movie" parameterType="int">
		SELECT m.no no, title,opendate,
		(SELECT NVL(AVG(score),0.0) score FROM reviews WHERE movie_no = 1) score,
		movie_rating movieRating, runtime, contents,t.src src,poster,m.regdate regdate
		FROM movies m, trailers t
		WHERE m.no = t.movie_no AND m.no = #{no}
	</select>
</mapper>