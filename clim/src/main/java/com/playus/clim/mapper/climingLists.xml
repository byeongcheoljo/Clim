<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="climingLists">
	<select id="subscribesLiveCheck" resultType="int" parameterType="int">
		SELECT count(*) 
		FROM c_lists
		WHERE close_time is not NULL AND member_no = #{memberNo}
	</select>
	<update id="updateSessionId" parameterType="ClimingList">
	update c_lists set session_id=#{sessionId}
	where no=#{no}
	</update>
</mapper>