<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="movies">
	<select id="searchMovieListForCliming" resultType="Movie" parameterType="String">
		SELECT no, title, runtime, poster,src
		FROM movies
		WHERE title like #{title}
	</select>
	<select id="getClimingMovieList" parameterType="int" resultType="Movie">
	select no,title,runtime,src,poster
	from movies
	where no in(select movie_no
	from bookmarks,
	(select no,member_no,title 
	from c_lists) clist
	where bookmarks.member_no=clist.member_no and clist.no=#{no} and type='C')
	</select>
</mapper>